<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - ILISAN Admin</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    
    <!-- Inter Font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600&display=swap" rel="stylesheet">
    
    <!-- Custom Admin CSS -->
    <link href="/css/admin.css" rel="stylesheet">

    <style>
        .rounded-bottom-custom {
            border-bottom-left-radius: 0.75rem;
            border-bottom-right-radius: 0.75rem;
            background-color: #f8f9fa;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
        }

        .admin-section-title {
            font-family: 'Poppins', sans-serif;
            font-size: 1.5rem;
            font-weight: 600;
            color: #2c3e50;
            letter-spacing: 0.5px;
            text-align: center;
            position: relative;
            display: inline-block;
            padding-bottom: 8px;
            margin-bottom: 1rem;
        }
        
        .admin-section-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 15%;
            width: 70%;
            height: 3px;
            background-color: #f39c12;
            border-radius: 2px;
        }
    </style>
</head>
<body class="admin-layout">
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <nav class="col-md-3 col-lg-2 d-md-block admin-sidebar collapse" id="sidebarMenu">
                <div class="position-sticky pt-3">
                    <div class="text-center mb-4 px-3">
                        <div class="mb-3">
                            <a href="/" target="_blank">
                                <img src="/logo.png" alt="ILISAN" class="logo-admin mb-2" style="background-color: #222222; max-height: 40px; width: auto;" onerror="this.style.display='none';">
                            </a>
                            <h5 class="text-dark mb-0 fw-bold">🛡️ ILISAN</h5>
                            <small class="text-muted fw-medium">Admin Paneli</small>
                        </div>
                        <div class="admin-user-info p-3 rounded-3" style="background: rgba(99, 102, 241, 0.06); border: 1px solid rgba(99, 102, 241, 0.1);">
                            <div class="d-flex align-items-center justify-content-center">
                                <div class="admin-avatar me-3">
                                    <i class="fas fa-user-shield"></i>
                                </div>
                                <div class="text-start" style="font-size: 0.85rem;">
                                    <div class="text-dark fw-semibold">Yönetici</div>
                                    <div class="text-muted small d-flex align-items-center">
                                        <span class="badge bg-success-subtle text-success me-1" style="width: 8px; height: 8px; border-radius: 50%; padding: 0;"></span>
                                        Online
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <ul class="nav flex-column px-2">
                        <!-- Ana Menü -->
                        <li class="nav-item mb-1">
                            <a class="nav-link @(ViewContext.RouteData.Values["Action"]?.ToString() == "Index" && ViewContext.RouteData.Values["Controller"]?.ToString() == "Admin" ? "active" : "")" 
                               href="@Url.Action("Index", "Admin")" data-bs-toggle="tooltip" data-bs-placement="right" title="Ana Sayfa">
                                <i class="fas fa-tachometer-alt me-2"></i>
                                <span class="nav-text">Dashboard</span>
                            </a>
                        </li>
                        
                        <!-- E-ticaret Yönetimi -->
                        <li class="nav-header mt-3 mb-2">
                            <small class="text-muted text-uppercase fw-bold">E-TİCARET</small>
                        </li>
                        
                        <li class="nav-item mb-1">
                            <a class="nav-link @(ViewContext.RouteData.Values["Action"]?.ToString() == "Products" ? "active" : "")" 
                               href="@Url.Action("Products", "Admin")" data-bs-toggle="tooltip" data-bs-placement="right" title="Ürün Yönetimi">
                                <i class="fas fa-cube me-2"></i>
                                <span class="nav-text">Ürünler</span>
                                <span class="badge bg-primary badge-pill ms-auto">New</span>
                            </a>
                        </li>
                        
                        <li class="nav-item mb-1">
                            <a class="nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Categories" ? "active" : "")" 
                               href="@Url.Action("Index", "Categories")" data-bs-toggle="tooltip" data-bs-placement="right" title="Kategori Yönetimi">
                                <i class="fas fa-tags me-2"></i>
                                <span class="nav-text">Kategoriler</span>
                            </a>
                        </li>
                        
                        <li class="nav-item mb-1">
                            <a class="nav-link @(ViewContext.RouteData.Values["Action"]?.ToString() == "Orders" ? "active" : "")" 
                               href="@Url.Action("Orders", "Admin")" data-bs-toggle="tooltip" data-bs-placement="right" title="Sipariş Yönetimi">
                                <i class="fas fa-shopping-cart me-2"></i>
                                <span class="nav-text">Siparişler</span>
                                @await Component.InvokeAsync("OrderCount")
                            </a>
                        </li>
                        
                        <li class="nav-item mb-1">
                            <a class="nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Customers" ? "active" : "")" 
                               href="@Url.Action("Index", "Customers")" data-bs-toggle="tooltip" data-bs-placement="right" title="Müşteri Yönetimi">
                                <i class="fas fa-users me-2"></i>
                                <span class="nav-text">Müşteriler</span>
                                <span class="badge bg-info badge-pill ms-auto">New</span>
                            </a>
                        </li>
                        
                        <!-- İçerik Yönetimi -->
                        <li class="nav-header mt-3 mb-2">
                            <small class="text-muted text-uppercase fw-bold">İÇERİK</small>
                        </li>
                        
                        <li class="nav-item mb-1">
                            <a class="nav-link @(ViewContext.RouteData.Values["Action"]?.ToString() == "Sliders" ? "active" : "")" 
                               href="@Url.Action("Sliders", "Admin")" data-bs-toggle="tooltip" data-bs-placement="right" title="Slider Yönetimi">
                                <i class="fas fa-images me-2"></i>
                                <span class="nav-text">Slider</span>
                            </a>
                        </li>
                        
                        <!-- Sistem Ayarları -->
                        <li class="nav-header mt-3 mb-2">
                            <small class="text-muted text-uppercase fw-bold">SİSTEM</small>
                        </li>
                        
                        <li class="nav-item mb-1">
                            <a class="nav-link @(ViewContext.RouteData.Values["Action"]?.ToString() == "ShippingRates" ? "active" : "")" 
                               href="@Url.Action("ShippingRates", "Admin")" data-bs-toggle="tooltip" data-bs-placement="right" title="Kargo Ayarları">
                                <i class="fas fa-shipping-fast me-2"></i>
                                <span class="nav-text">Kargo</span>
                            </a>
                        </li>
                        
                        <li class="nav-item mb-1">
                            <a class="nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Reports" ? "active" : "")" 
                               href="@Url.Action("Index", "Reports")" data-bs-toggle="tooltip" data-bs-placement="right" title="Raporlar ve Analizler">
                                <i class="fas fa-chart-line me-2"></i>
                                <span class="nav-text">Raporlar</span>
                            </a>
                        </li>
                        
                        <li class="nav-item mb-1">
                            <a class="nav-link @(ViewContext.RouteData.Values["Action"]?.ToString() == "SiteSettings" ? "active" : "")" 
                               href="@Url.Action("SiteSettings", "Admin")" data-bs-toggle="tooltip" data-bs-placement="right" title="Site Ayarları">
                                <i class="fas fa-cogs me-2"></i>
                                <span class="nav-text">Site Ayarları</span>
                            </a>
                        </li>
                        
                        <li class="nav-item mb-1">
                            <a class="nav-link @(ViewContext.RouteData.Values["Action"]?.ToString() == "TestEmail" ? "active" : "")" 
                               href="@Url.Action("TestEmail", "Admin")" data-bs-toggle="tooltip" data-bs-placement="right" title="Test Email Gönder">
                                <i class="fas fa-envelope me-2"></i>
                                <span class="nav-text">Test Email</span>
                            </a>
                        </li>
                        
                        <!-- Hızlı Erişim -->
                        <li class="nav-header mt-4 mb-2">
                            <small class="text-muted text-uppercase fw-bold">HIZLI ERİŞİM</small>
                        </li>
                        
                        <li class="nav-item mb-1">
                            <a class="nav-link" href="@Url.Action("Index", "Home")" target="_blank" data-bs-toggle="tooltip" data-bs-placement="right" title="Siteyi Yeni Sekmede Aç">
                                <i class="fas fa-external-link-alt me-2"></i>
                                <span class="nav-text">Siteyi Gör</span>
                            </a>
                        </li>
                        
                        <!-- Çıkış -->
                        <li class="nav-item mt-4 border-top pt-3">
                            <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                                <button type="submit" class="nav-link text-danger bg-transparent border-0 w-100 text-start" data-bs-toggle="tooltip" data-bs-placement="right" title="Güvenli Çıkış" style="background: none !important;">
                                    <i class="fas fa-sign-out-alt me-2"></i>
                                    <span class="nav-text">Çıkış Yap</span>
                                </button>
                            </form>
                        </li>
                    </ul>
                </div>
            </nav>

            <!-- Main content -->
            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
                <!-- Topbar -->
                <div class="admin-topbar rounded-bottom-custom d-flex flex-column align-items-center text-center py-3 mb-3">
                    <h1 class="h4 admin-section-title mb-0">@ViewData["Title"]</h1>
                    <div class="btn-toolbar mb-2 mb-md-0">
                    </div>
                </div>

                <!-- Alert Messages -->
                @if (TempData["Success"] != null)
                {
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <i class="fas fa-check-circle"></i> @TempData["Success"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }

                @if (TempData["Error"] != null)
                {
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <i class="fas fa-exclamation-circle"></i> @TempData["Error"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }

                @if (TempData["Warning"] != null)
                {
                    <div class="alert alert-warning alert-dismissible fade show" role="alert">
                        <i class="fas fa-exclamation-triangle"></i> @TempData["Warning"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }

                @if (TempData["Info"] != null)
                {
                    <div class="alert alert-info alert-dismissible fade show" role="alert">
                        <i class="fas fa-info-circle"></i> @TempData["Info"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }

                <!-- Page Content -->
                @RenderBody()
            </main>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <script>
        $(document).ready(function() {
            // Initialize tooltips
            $('[data-bs-toggle="tooltip"]').tooltip();
            
            // Mobile sidebar toggle
            $('#sidebarToggle').on('click', function() {
                $('#sidebarMenu').toggleClass('show');
                $('.sidebar-overlay').toggleClass('show');
            });
            
            // Close sidebar on overlay click
            $('.sidebar-overlay').on('click', function() {
                $('#sidebarMenu').removeClass('show');
                $(this).removeClass('show');
            });
            
            // Auto-hide alerts after 5 seconds
            $('.alert').each(function() {
                const alert = $(this);
                setTimeout(function() {
                    alert.fadeOut();
                }, 5000);
            });
            
            // Add loading state to forms
            $('form').on('submit', function() {
                const submitBtn = $(this).find('button[type="submit"]');
                if (!submitBtn.hasClass('btn-loading')) {
                    submitBtn.addClass('btn-loading').prop('disabled', true);
                }
            });
            
            // Confirmation dialogs
            $('[data-confirm]').on('click', function(e) {
                const message = $(this).data('confirm');
                if (!confirm(message)) {
                    e.preventDefault();
                    return false;
                }
            });
            
            // Status toggle animations
            $('.status-toggle').on('click', function() {
                const button = $(this);
                const originalHtml = button.html();
                button.html('<i class="fas fa-spinner fa-spin"></i>').prop('disabled', true);
                
                setTimeout(function() {
                    button.html(originalHtml).prop('disabled', false);
                }, 1000);
            });
        });
        
        // Global toast notification function
        window.showToast = function(message, type = 'info') {
            const toastId = 'toast-' + Date.now();
            const bgClass = type === 'success' ? 'bg-success' : type === 'error' ? 'bg-danger' : 'bg-info';
            
            const toastHtml = `
                <div id="${toastId}" class="toast align-items-center text-white ${bgClass} border-0" role="alert" aria-live="assertive" aria-atomic="true">
                    <div class="d-flex">
                        <div class="toast-body">
                            <i class="fas fa-${type === 'success' ? 'check-circle' : type === 'error' ? 'exclamation-circle' : 'info-circle'} me-2"></i>
                            ${message}
                        </div>
                        <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button>
                    </div>
                </div>
            `;
            
            let toastContainer = $('#toast-container');
            if (toastContainer.length === 0) {
                toastContainer = $('<div id="toast-container" class="toast-container position-fixed top-0 end-0 p-3" style="z-index: 1055;"></div>');
                $('body').append(toastContainer);
            }
            
            toastContainer.append(toastHtml);
            const toastElement = new bootstrap.Toast(document.getElementById(toastId), {
                autohide: true,
                delay: 4000
            });
            toastElement.show();
            
            // Remove toast element after it's hidden
            $(`#${toastId}`).on('hidden.bs.toast', function() {
                $(this).remove();
            });
        };
    </script>
    <script src="/js/admin-product-variants.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
